export const NOT_REPORTING = 'Not Reporting Errors';

export class ValidationReporter {
  renderErrors(errors) {
    return NOT_REPORTING;
  }
  unrenderErrors(errors) {
    return NOT_REPORTING;
  }
}
